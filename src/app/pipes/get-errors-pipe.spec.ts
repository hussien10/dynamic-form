import { GetErrorsPipe } from './get-errors-pipe';

describe('GetErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
